/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Optional: If you ever switch to the Next.js <Image> component, 
  // you will need to disable its built-in server-side optimization for GitHub Pages:
  // images: { unoptimized: true }
};

export default nextConfig;
